# --- Core backend framework ---
fastapi==0.115.0
uvicorn==0.30.1

# --- Middleware / Utilities ---
python-dotenv
requests
httpx
pydantic
anyio

# --- CORS (for React frontend requests) ---
fastapi[all]

# --- Optional: If your backend talks to OpenAI API ---
openai

# --- Optional: If you call local Ollama server (http://localhost:11434) ---
# no extra package needed, just use `requests`
